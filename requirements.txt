discord.py>=2.3,<3
gspread>=6.0
google-auth>=2.28
PyYAML>=6.0
requests>=2.31
# render_cit_inventory.py runs under this same interpreter ({python} in gif.command),
# so Pillow has to be in this venv too.
Pillow>=10.0
